#include "testRun.h"
#include  "GameObjectManager.h"
/// <summary>
/// This demonstrates a running parallax background where after X seconds, a batch of assets will be streamed and loaded.
/// </summary>
//const sf::Time testRun::TIME_PER_FRAME = sf::seconds(1.f / 60.f);

testRun::testRun()
	//window(sf::VideoMode(WINDOW_WIDTH, WINDOW_HEIGHT), "Khalil Jan Arcilla")
	{
	//load initial textures

}
